
# ===============================
# DATABASE
# ===============================
spring.datasource.url=jdbc:mysql://localhost/ms_machine?serverTimezone=Africa/Douala&zeroDateTimeBehavior=convertToNull&createDatabaseIfNotExist=true&autoReconnect=true&useSSL=false
spring.datasource.username=root
spring.datasource.password =12345Celesto#
spring.datasource.platform=mysql
spring.datasource.initialization-mode=always
spring.datasource.sql-script-encoding=UTF-8

# ===============================
# JPA / HIBERNATE
# ===============================
spring.jpa.show-sql=true
# Hibernate ddl auto (none, create, create-drop, update)
spring.jpa.hibernate.ddl-auto=update
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect

# ===============================
# SERVEUR
# ===============================
server.port=8080
spring.application.name=ms-gestion-machine

# ===============================
# PROFIL
# ===============================
#dev : activ� par d�faut en mode dev.
#test : activ� par d�faut dans les tests unitaires.
#prod : activ� par d�faut quand on est ni en dev ni en test.


